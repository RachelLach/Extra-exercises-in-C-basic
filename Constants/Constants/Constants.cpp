#include <iostream>

int main() {
	const double pi = 3.14;  // Declare a variable of type double call it pi ->
	return 0;                // and set it to 3.14 
	                         // we write const before declare the variable 
}

// so now if I want to change the value of pi later in my code i get an error

#include <iostream>
using namespace std;

int main()
{
   
}

// to declare a variable we need to specify its type 
// statically-typed languages: C++, C#, Java and TypeScript ->
// when declare a variable, I need to specify its type and this type cannot ->
// change throughout the lifetime of my program. 

// #

// dynamically-type languages: Python, Javascript and Ruby -> 
// in these languagaes I do not need to give a variable particular type ->
// the type will be determined based on the value that we assigned to these
// variables and that type can change throughout the lifetime of my program

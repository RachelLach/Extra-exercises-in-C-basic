#include <iostream>

using namespace std;

int main()
{
    char ch = 'a'; // declare a char called ch and set to single quotes and between them a
    cout << +ch; // write + to get the numeric presentation of lowercase a 
    return 0;
}


/* char -> for storing a single character */
/* string -> for storing a sequence of characters */